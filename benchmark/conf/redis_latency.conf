# redis_latency
# fields: sessions (concurrency), msgs (requests per session), msg_bytes (ECHO payload size)
ITERATIONS=3
WARMUP=1
TIMEOUT_SEC=90
SCENARIO_ROWS=(
  "sessions=1 msgs=5000 msg_bytes=16"
  "sessions=8 msgs=1000 msg_bytes=16"
  "sessions=32 msgs=300 msg_bytes=64"
  "sessions=32 msgs=300 msg_bytes=1024"
)
