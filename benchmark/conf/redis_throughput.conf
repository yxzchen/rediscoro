# redis_throughput
# fields: sessions (connections), total_ops_per_session (commands per connection), inflight (pipeline batch size)
ITERATIONS=3
WARMUP=1
TIMEOUT_SEC=120
SCENARIO_ROWS=(
  "sessions=1 total_ops_per_session=200000 inflight=1"
  "sessions=8 total_ops_per_session=50000 inflight=8"
  "sessions=32 total_ops_per_session=20000 inflight=32"
)
