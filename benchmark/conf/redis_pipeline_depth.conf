# redis_pipeline_depth
# fields: sessions (connections), total_cmds_per_session (commands per connection), pipeline_depth (commands per batch)
ITERATIONS=3
WARMUP=1
TIMEOUT_SEC=120
SCENARIO_ROWS=(
  "sessions=8 total_cmds_per_session=80000 pipeline_depth=1"
  "sessions=8 total_cmds_per_session=80000 pipeline_depth=8"
  "sessions=8 total_cmds_per_session=80000 pipeline_depth=64"
)
