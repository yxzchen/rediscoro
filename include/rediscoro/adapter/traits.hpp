#pragma once

namespace rediscoro::adapter {

// Customization points for struct/tuple bindings can live here.
// Intentionally empty for now: adapter core does not require traits.

}  // namespace rediscoro::adapter


